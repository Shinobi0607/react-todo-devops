apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-app-svc
spec:
  selector:
    app: {{ .Release.Name }}-todo-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  type: {{ .Values.service.type }}
